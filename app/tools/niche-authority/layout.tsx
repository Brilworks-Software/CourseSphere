export const metadata = {
  title: "Niche Authority Score | CourseSphere",
  description: "Kill imposter syndrome with data. Discover if you're expert enough to sell a course.",
};

export default function NicheAuthorityLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
