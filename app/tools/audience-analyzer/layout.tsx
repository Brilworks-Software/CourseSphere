export const metadata = {
  title: "Audience Skill Level Analyzer | CourseSphere",
  description: "Discover who your course should be for. Analyze your audience's skill level to avoid the 'too broad course' mistake.",
};

export default function AudienceAnalyzerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
